targets=(
    "fedora-30"
)
pkgname="imgurbash2"
pkgver="3.1"
pkgrel="1"
pkgdesc="imgurbash2 is a simple bash script that allows you to upload images to imgur"
pkgdesclong=(
    "imgurbash2 is a simple bash script that allows you to upload images to imgur"
)
maintainer="HeyLazySunnyKid"
arch="all"
license=("GPL")
depends:fedora-30=(
    "xsel"
)
url="https://github.com/DaveDavenport/xininfo"
section="utils"
sources=(
    "imgurbash2"
)
hashsums=(
    "SKIP"
)

build() {
}

package() {
    install -vDm 755 "imgurbash2" "${pkgdir}/usr/local/bin/imgurbash2"
}
